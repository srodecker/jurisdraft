node_modules/
*.log
.DS_Store
.env
dist/
build/
tmp/
# Attorney private files — never commit these
Signatures/

# Old single-user config (migrated to profile system)
firm-config.json

# Case data — sensitive legal data, never commit
matters/
data/notifications.json
